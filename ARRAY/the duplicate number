#include <stdio.h>

int main() {
    int n;
    int arr[100];
    int freq[101] = {0};  // Frequency array
    int duplicate = -1;

    // Read number of elements
    printf("Enter number of elements: ");
    scanf("%d", &n);

    // Read array elements
    printf("Enter array elements (range 1 to %d):\n", n - 1);
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    // Find duplicate element
    for (int i = 0; i < n; i++) {
        freq[arr[i]]++;
        if (freq[arr[i]] > 1) {
            duplicate = arr[i];
            break;
        }
    }

    // Display result
    if (duplicate != -1) {
        printf("Duplicate number: %d\n", duplicate);
    } else {
        printf("No duplicate number found.\n");
    }

    return 0;
}
